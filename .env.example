# MongoDB Connection String (Required for Database)
MONGODB_URI="mongodb://localhost:27017/bhoomisetu?retryWrites=true"

# Server Port (Default is 3000)
PORT="3000"

# JWT Secret
JWT_SECRET="YOUR_SUPER_SECRET_JWT_KEY"

# Official Registration Secret Key (Restricted Government Invite-Only)
REGISTRATION_SECRET_KEY="BHOOMISETU_OFFICIAL_2026"

# OAuth SSO Provider Keys (Placeholders)
VITE_GOOGLE_CLIENT_ID="placeholder-google-client-id.apps.googleusercontent.com"

# SMTP Configuration for Email OTP
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="user@example.com"
SMTP_PASS="smtp_password"

# Mapbox Access Token for GIS integration (Public)
VITE_MAPBOX_TOKEN=""

# Gemini API Key for AI Insights
GEMINI_API_KEY=""
